--extra-index-url https://download.pytorch.org/whl/cpu

# Core graph + orchestration
langchain==0.3.7
langchain-community==0.3.7
langchain-core==0.3.19
langgraph==0.2.53

# LLM provider — Groq
langchain-groq==0.2.1

# Embeddings (local, no API key needed)
torch
sentence-transformers==3.3.1

# Vector store
chromadb==0.5.23
langchain-chroma==0.1.4
# Chroma 0.5.x uses PostHog's legacy capture API; newer major versions break it.
posthog<6

# Graph structure + query
networkx==3.4.2

# Schema / validation
pydantic==2.10.2

# Notebook / CLI quality-of-life
python-dotenv==1.0.1
pandas==2.2.3
tqdm==4.67.1
matplotlib==3.9.3
pyvis==0.3.2

# Optional: expose query layer as an APIs
fastapi==0.115.6
uvicorn==0.32.1
httpx==0.28.1
python-multipart==0.0.20
pypdf==5.1.0

# Optional: if you want to work in Jupyter too
jupyterlab==4.3.2
